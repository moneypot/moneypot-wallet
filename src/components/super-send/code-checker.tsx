import React from 'react';

export function isLightning(code: string) {
  const str = code.toLowerCase();
  return str[0]==='l'

}

export function hasAmountSet(code: number) {
  console.log('to do');

}